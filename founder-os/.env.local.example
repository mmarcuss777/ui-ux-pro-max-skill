NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # server only, never exposed to client
ANTHROPIC_API_KEY=                # server only
NEXT_PUBLIC_VAPID_PUBLIC_KEY=     # web push (npx web-push generate-vapid-keys)
VAPID_PRIVATE_KEY=                # server only
CRON_SECRET=                      # protects /api/cron/* (any long random string)
INTEGRATION_TOKEN_KEY=            # 32 bytes base64: openssl rand -base64 32
STRAVA_CLIENT_ID=                 # strava.com/settings/api
STRAVA_CLIENT_SECRET=
